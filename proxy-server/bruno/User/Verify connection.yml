info:
  name: Verify connection
  type: http
  seq: 5

http:
  method: GET
  url: "{{api_url}}/api/users/verify"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
