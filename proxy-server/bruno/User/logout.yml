info:
  name: logout
  type: http
  seq: 6

http:
  method: POST
  url: "{{api_url}}/api/users/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
