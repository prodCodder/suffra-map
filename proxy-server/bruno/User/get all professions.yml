info:
  name: get all professions
  type: http
  seq: 7

http:
  method: GET
  url: "{{api_url}}/api/users/profession"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
